# TypeScript build output
node_modules/
dist/
*.js.map

# Go binary (removed — project is now TypeScript-only)
dispatch
dispatch-linux-*

# Runtime data
jobs/active/*
jobs/done/*
jobs/failed/*
logs/*
!jobs/**/.gitkeep
!logs/.gitkeep

# Local config (use .example files as templates)
models.json
agents.json
config.json
state.json

# Local prompts (use .example files as templates)
prompts/*.md
!prompts/*.md.example
dispatch-linux
